# Finished instructor reference

This is the complete bounded agent and the minute-45 recovery app. Configure
the model using [`../MODEL-SETUP.md`](../MODEL-SETUP.md), run `npm test`, then
`npm run dev -- --port 3001`. Use Sam's vault for the guaranteed demo.
