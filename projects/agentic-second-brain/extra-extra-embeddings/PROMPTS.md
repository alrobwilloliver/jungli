# Embeddings follow-up

Ask: “Design an offline experiment comparing lexical candidates with embedding
candidates. Keep exact paths, use the existing `read_note` as the only full-note
reader, include stale-index detection, and define a small retrieval evaluation.
Do not change the 60-minute classroom projects.”
