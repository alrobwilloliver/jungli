# macOS
.DS_Store

# Local Claude Code settings (machine-specific, not shared)
.claude/settings.local.json

# Raw survey export — contains applicant contact details (email/phone).
# Keep local only; the sanitised markdown extract lives in source/survey-responses.md
source/*.xlsx

# Obsidian vault config (created when a vault folder is opened in Obsidian)
.obsidian/

# Node (for when the projects run)
node_modules/
.env
.env.local
